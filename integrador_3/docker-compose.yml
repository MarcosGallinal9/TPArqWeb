version: '3.8'

services:
  integrador_db:
    image: mysql:8.0
    container_name: integrador_3
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: integrador_3
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql